
module.exports = {
  db: {
    host:  '127.0.0.1',      // Default to localhost if not set
    user: 'root',           // Default to root if not set
    password:  '',       // Default to empty string if not set
    database: '', // Default to grocery_db if not set
  },
  // You can add other configurations (like server settings, logging settings) here
};
